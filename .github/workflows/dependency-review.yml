name: 依赖审查

on:
  pull_request:

permissions:
  contents: read

jobs:
  dependency-review:
    name: 检查新增依赖风险
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v7

      - name: 审查依赖变更
        uses: actions/dependency-review-action@v5
